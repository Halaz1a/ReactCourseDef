import "../App.css"

function Case({symbole}) {
    //const [croix, setCroix] = useState()

    //onClick={setCroix(symbole)}
    //{ croix ? "X" : "O"}
    return (
        <div className="case"></div>
    );
}

export default Case